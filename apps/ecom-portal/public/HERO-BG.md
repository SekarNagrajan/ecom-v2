# Public landing backgrounds

| File | When used |
|------|-----------|
| `landing-bg-light.png` | Light mode (default) |
| `hero-bg.png` | Dark mode (`html.dark`) |

Switched via CSS in `global-theme-styles.tsx` (`.pub-layout-bg` / `html.dark .pub-layout-bg`).
